using Music; // directive to use the Music namespace declared in the file Music.cs

// create a new note using the struct Note in the Music namespace


// Add solutions to your tasks here

bool isStudent = true;
bool isStudying = false;
if (isStudent){
    isStudying = true;
}
else
{
    isStudying = false;
}
